[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-agent-os"
version = "0.1.0"
description = "A persistent Codex execution supervisor and local dashboard for WSL2"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.package-data]
agent_os = ["static/*"]
