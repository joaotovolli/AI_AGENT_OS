# Project workspace

Place deliverables and project code for user goals here. The agent may reorganize this directory
to suit a concrete project. Keep operating instructions in `docs/` and host setup in `infra/`.

This folder starts empty of project-specific work so the repository can serve as a reusable base.
